# Databricks service principal
DATABRICKS_HOST=your-workspace.cloud.databricks.com
DATABRICKS_CLIENT_ID=your-service-principal-client-id
DATABRICKS_CLIENT_SECRET=your-service-principal-secret

# Lakebase Postgres endpoint
# Format: projects/<id>/branches/<id>/endpoints/<id>
# Get it by running: databricks postgres list-projects / list-branches / list-endpoints
LAKEBASE_ENDPOINT=projects/<project-id>/branches/<branch-id>/endpoints/<endpoint-id>

# Lakebase connection details (from the endpoint's status.hosts.host field)
LAKEBASE_HOST=your-endpoint.database.<region>.cloud.databricks.com
LAKEBASE_PORT=5432
LAKEBASE_DATABASE=databricks_postgres
